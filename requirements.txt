openai>=1.66.0
requests
httpx
dulwich
starlette
python-multipart
uvicorn[standard]
websockets
huggingface_hub
PyYAML>=6.0
pytest>=7.0
claude-agent-sdk>=0.1.60
# MCP (Model Context Protocol) client. The runtime guards imports so a
# broken/minimal source checkout reports sdk_available=false instead of
# crashing, but packaged/source installs include the SDK by default.
mcp>=1.6,<2.0
# optional: browser automation tools (for browse_page/browser_action)
playwright
playwright-stealth
# optional: local model server (requires Metal-enabled build on macOS)
# CMAKE_ARGS="-DGGML_METAL=on" pip install llama-cpp-python[server]
# OpenTelemetry tracing (OpenInference conventions). Runtime degrades to a
# no-op if these packages are missing.
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0
# NVIDIA NeMo Guardrails — input/output policy rails on LLM calls. The
# runtime falls back to a no-op when the package is absent or when
# OUROBOROS_NEMO_CONFIG_PATH is not set.
nemoguardrails>=0.11.0
